import 'package:flutter/material.dart';

class OMContactsWidget extends StatelessWidget {
  const OMContactsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("CONTACTS SCREEN");
  }
}
