import 'package:flutter/material.dart';

class OMPortfolioWidget extends StatelessWidget {
  const OMPortfolioWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("PORTFOLIO SCREEN");
  }
}
